import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors
  static const Color primary = Color(0xFFE11D48); // Red
  static const Color primaryLight = Color(0xFFFECDD3);
  static const Color primaryDark = Color(0xFF881337);

  // Secondary Colors
  static const Color secondary = Color(0xFFF97316); // Orange
  static const Color secondaryLight = Color(0xFFFED7AA);
  static const Color secondaryDark = Color(0xFFEA580C);

  // Emergency Colors
  static const Color emergency = Color(0xFFDC2626); // Red
  static const Color warning = Color(0xFFF59E0B); // Amber
  static const Color success = Color(0xFF059669); // Green
  static const Color info = Color(0xFF0284C7); // Blue

  // Harassment specific
  static const Color harassment = Color(0xFF9333EA); // Purple
  static const Color harassmentLight = Color(0xFFF3E8FF);

  // Disaster specific
  static const Color disaster = Color(0xFFF97316); // Orange
  static const Color disasterLight = Color(0xFFFED7AA);

  // Neutral Colors
  static const Color background = Color(0xFFF8F9FA);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceVariant = Color(0xFFF1F5F9);

  // Text Colors
  static const Color onPrimary = Color(0xFFFFFFFF);
  static const Color onSurface = Color(0xFF1F2937);
  static const Color onSurfaceVariant = Color(0xFF6B7280);
  static const Color onBackground = Color(0xFF111827);

  // Status Colors
  static const Color error = Color(0xFFDC2626);
  static const Color onError = Color(0xFFFFFFFF);

  // Border Colors
  static const Color border = Color(0xFFE5E7EB);
  static const Color borderFocus = Color(0xFFE11D48);

  // Shadow Colors
  static const Color shadow = Color(0x1A000000);
  static const Color shadowLight = Color(0x0D000000);
}
